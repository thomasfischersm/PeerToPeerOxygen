package com.playposse.peertopeeroxygen.android.data.types;

/**
 * An enum that describes the role a user plays in a mission.
 */
public enum UserMissionRoleType {
    student,
    buddy,
    superBuddy,
}
